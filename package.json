{
  "name": "@zimtsui/shiftable",
  "version": "0.0.1",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b ./src/tsconfig.json",
    "clean": "rm -rf ./build"
  },
  "author": "Zim",
  "dependencies": {
    "@types/assert": "^1.5.6",
    "@zimtsui/binary-heap": "^0.0.2",
    "assert": "^1.5.0"
  }
}
